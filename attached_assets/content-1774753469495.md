[Skip to main content](https://actualbudget.org/docs/api/reference/#__docusaurus_skipToContent_fallback)

On this page

Budgets

- [getBudgetMonths](https://actualbudget.org/docs/api/reference/#getbudgetmonths)
- [getBudgetMonth](https://actualbudget.org/docs/api/reference/#getbudgetmonth)
- [setBudgetAmount](https://actualbudget.org/docs/api/reference/#setbudgetamount)
- [setBudgetCarryover](https://actualbudget.org/docs/api/reference/#setbudgetcarryover)
- [holdBudgetForNextMonth](https://actualbudget.org/docs/api/reference/#holdbudgetfornextmonth)
- [resetBudgetHold](https://actualbudget.org/docs/api/reference/#resetbudgethold)

Transactions

- [Transaction](https://actualbudget.org/docs/api/reference/#transaction)
- [addTransactions](https://actualbudget.org/docs/api/reference/#addtransactions)
- [importTransactions](https://actualbudget.org/docs/api/reference/#importtransactions)
- [getTransactions](https://actualbudget.org/docs/api/reference/#gettransactions)
- [updateTransaction](https://actualbudget.org/docs/api/reference/#updatetransaction)
- [deleteTransaction](https://actualbudget.org/docs/api/reference/#deletetransaction)

Accounts

- [Account](https://actualbudget.org/docs/api/reference/#account)
- [getAccounts](https://actualbudget.org/docs/api/reference/#getaccounts)
- [createAccount](https://actualbudget.org/docs/api/reference/#createaccount)
- [updateAccount](https://actualbudget.org/docs/api/reference/#updateaccount)
- [closeAccount](https://actualbudget.org/docs/api/reference/#closeaccount)
- [reopenAccount](https://actualbudget.org/docs/api/reference/#reopenaccount)
- [deleteAccount](https://actualbudget.org/docs/api/reference/#deleteaccount)
- [getAccountBalance](https://actualbudget.org/docs/api/reference/#getaccountbalance)

Categories

- [Category](https://actualbudget.org/docs/api/reference/#category)
- [getCategories](https://actualbudget.org/docs/api/reference/#getcategories)
- [createCategory](https://actualbudget.org/docs/api/reference/#createcategory)
- [updateCategory](https://actualbudget.org/docs/api/reference/#updatecategory)
- [deleteCategory](https://actualbudget.org/docs/api/reference/#deletecategory)

Category Groups

- [Category group](https://actualbudget.org/docs/api/reference/#category-group)
- [getCategoryGroups](https://actualbudget.org/docs/api/reference/#getcategorygroups)
- [createCategoryGroup](https://actualbudget.org/docs/api/reference/#createcategorygroup)
- [updateCategoryGroup](https://actualbudget.org/docs/api/reference/#updatecategorygroup)
- [deleteCategoryGroup](https://actualbudget.org/docs/api/reference/#deletecategorygroup)

Payees

- [Payee](https://actualbudget.org/docs/api/reference/#payee)
- [getPayees](https://actualbudget.org/docs/api/reference/#getpayees)
- [createPayee](https://actualbudget.org/docs/api/reference/#createpayee)
- [updatePayee](https://actualbudget.org/docs/api/reference/#updatepayee)
- [deletePayee](https://actualbudget.org/docs/api/reference/#deletepayee)
- [mergePayees](https://actualbudget.org/docs/api/reference/#mergepayees)

Tags

- [Tag](https://actualbudget.org/docs/api/reference/#tag)
- [getTags](https://actualbudget.org/docs/api/reference/#gettags)
- [createTag](https://actualbudget.org/docs/api/reference/#createtag)
- [updateTag](https://actualbudget.org/docs/api/reference/#updatetag)
- [deleteTag](https://actualbudget.org/docs/api/reference/#deletetag)

Rules

- [ConditionOrAction](https://actualbudget.org/docs/api/reference/#conditionoraction)
- [Rule](https://actualbudget.org/docs/api/reference/#rule)
- [Payee rule](https://actualbudget.org/docs/api/reference/#payee-rule)
- [getRules](https://actualbudget.org/docs/api/reference/#getrules)
- [getPayeeRules](https://actualbudget.org/docs/api/reference/#getpayeerules)
- [createRule](https://actualbudget.org/docs/api/reference/#createrule)
- [updateRule](https://actualbudget.org/docs/api/reference/#updaterule)
- [deleteRule](https://actualbudget.org/docs/api/reference/#deleterule)

Schedules

- [Schedule](https://actualbudget.org/docs/api/reference/#schedule)
- [RecurConfig](https://actualbudget.org/docs/api/reference/#recurconfig)
- [getSchedules](https://actualbudget.org/docs/api/reference/#getschedules)
- [createSchedule](https://actualbudget.org/docs/api/reference/#createschedule)
- [updateSchedule](https://actualbudget.org/docs/api/reference/#updateschedule)
- [deleteSchedule](https://actualbudget.org/docs/api/reference/#deleteschedule)

Misc

- [BudgetFile](https://actualbudget.org/docs/api/reference/#budgetfile)
- [initConfig](https://actualbudget.org/docs/api/reference/#initconfig)
- [init](https://actualbudget.org/docs/api/reference/#init)
- [shutdown](https://actualbudget.org/docs/api/reference/#shutdown)
- [sync](https://actualbudget.org/docs/api/reference/#sync)
- [runBankSync](https://actualbudget.org/docs/api/reference/#runbanksync)
- [runImport](https://actualbudget.org/docs/api/reference/#runimport)
- [getBudgets](https://actualbudget.org/docs/api/reference/#getbudgets)
- [loadBudget](https://actualbudget.org/docs/api/reference/#loadbudget)
- [downloadBudget](https://actualbudget.org/docs/api/reference/#downloadbudget)
- [batchBudgetUpdates](https://actualbudget.org/docs/api/reference/#batchbudgetupdates)
- [runQuery](https://actualbudget.org/docs/api/reference/#runquery)
- [getIDByName](https://actualbudget.org/docs/api/reference/#getidbyname)

## Types of Methods [​](https://actualbudget.org/docs/api/reference/\#types-of-methods "Direct link to Types of Methods")

API methods are categorized into one of four types:

- `get`
- `create`
- `update`
- `delete`

Objects may have fields specific for a type of method. For example, the `payee` field of a `transaction` is only available in a `create` method. This field doesn't exist in objects returned from a `get` method (`payee_id` is used instead).

Fields specific to a type of request are marked as such in the notes.

`id` is a special field. All objects have an `id` field. However, you don't need to specify an `id` in a `create` method; all `create` methods will return the created `id` back to you.

All `update` and `delete` methods take an `id` to specify the desired object. `update` takes the fields to update as a second argument — it does not take a full object. That means even if a field is required, you don't have to pass it to `update`. For example, a `category` requires the `group_id` field, however `updateCategory(id, { name: "Food" })` is a valid call. Required means that an `update` can't set the field to `null` and a `create` must always contain the field.

**Note:**`updateRule` is an exception — it requires the full [`Rule`](https://actualbudget.org/docs/api/reference/#rule) object including `id`, and returns `Promise<Rule>`.

## Primitives [​](https://actualbudget.org/docs/api/reference/\#primitives "Direct link to Primitives")

These are types.

| Name | Type | Notes |
| --- | --- | --- |
| `id` | `string` | [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) |
| `month` | `string` | `YYYY-MM` |
| `date` | `string` | `YYYY-MM-DD` |
| `amount` | `integer` | A currency amount is an integer representing the value without any decimal places. Usually it's `value * 100`, but it depends on your currency. For example, a USD amount of `$120.30` would be `12030`. |

## Budgets [​](https://actualbudget.org/docs/api/reference/\#budgets "Direct link to Budgets")

#### `getBudgetMonths` [​](https://actualbudget.org/docs/api/reference/\#getbudgetmonths "Direct link to getbudgetmonths")

`getBudgetMonths() → Promise<month[]>`

#### `getBudgetMonth` [​](https://actualbudget.org/docs/api/reference/\#getbudgetmonth "Direct link to getbudgetmonth")

`getBudgetMonth(monthmonth) → Promise<Budget>`

#### `setBudgetAmount` [​](https://actualbudget.org/docs/api/reference/\#setbudgetamount "Direct link to setbudgetamount")

`setBudgetAmount(monthmonth, idcategoryId, amountvalue) → Promise<null>`

#### `setBudgetCarryover` [​](https://actualbudget.org/docs/api/reference/\#setbudgetcarryover "Direct link to setbudgetcarryover")

`setBudgetCarryover(monthmonth, idcategoryId, boolflag) → Promise<null>`

#### `holdBudgetForNextMonth` [​](https://actualbudget.org/docs/api/reference/\#holdbudgetfornextmonth "Direct link to holdbudgetfornextmonth")

`holdBudgetForNextMonth(monthmonth, amountvalue) → Promise<null>`

#### `resetBudgetHold` [​](https://actualbudget.org/docs/api/reference/\#resetbudgethold "Direct link to resetbudgethold")

`resetBudgetHold(monthmonth) → Promise<null>`

## Transactions [​](https://actualbudget.org/docs/api/reference/\#transactions "Direct link to Transactions")

#### Transaction [​](https://actualbudget.org/docs/api/reference/\#transaction "Direct link to Transaction")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `account` | `id` | yes |  |
| `date` | `date` | yes |  |
| `amount` | `amount` | no |  |
| `payee` | `id` | no | In a [create](https://actualbudget.org/docs/api/reference/#types-of-methods) request, this overrides `payee_name`. |
| `payee_name` | `string` | no | If given, a payee will be created with this name. If this matches an already existing payee, that payee will be used.<br>\\* Only available in a [create](https://actualbudget.org/docs/api/reference/#types-of-methods) request |
| `imported_payee` | `string` | no | This can be anything. Meant to represent the raw description when importing, allowing the user to see the original value. |
| `category` | `id` | no |  |
| `notes` | `string` | no |  |
| `imported_id` | `string` | no | A unique id usually given by the bank, if importing. Use this to avoid duplicate transactions. |
| `transfer_id` | `string` | no | If a transfer, the `id` of the corresponding transaction in the other account. See [transfers](https://actualbudget.org/docs/api/reference/#transfers). |
| `cleared` | `boolean` | no | A flag indicating if the transaction has cleared or not. |
| `subtransactions` | `Transaction[]` | no | An array of subtransactions for a split transaction. See [split transactions](https://actualbudget.org/docs/api/reference/#split-transactions).<br>\\* Only available in a [get](https://actualbudget.org/docs/api/reference/#types-of-methods) or [create](https://actualbudget.org/docs/api/reference/#types-of-methods) request |

#### Split Transactions [​](https://actualbudget.org/docs/api/reference/\#split-transactions "Direct link to Split Transactions")

A split transaction has several sub-transactions that split the total
amount across them. You can create a split transaction by specifying
an array of sub-transactions in the `subtransactions` field.

Subtransactions can specify the following fields, and `amount` is the only required field:

- `amount`
- `category`
- `notes`

If the amounts of the sub-transactions do not equal the total amount
of the transaction, currently the API call will succeed but an error
will be displayed within the app.

#### Transfers [​](https://actualbudget.org/docs/api/reference/\#transfers "Direct link to Transfers")

Existing transfers will have the `transfer_id` field which points to the transaction on the other side. **You should not change this** or you will cause unexpected behavior. (You are allowed to set this when importing, however.)

If you want to create a transfer, use the transfer payee for the account you wish to transfer to/from. Load the payees, use the [`transfer_acct`](https://actualbudget.org/docs/api/reference/#payee) field of the payee to find the account you want to transfer to/from, and assign that payee to the transaction. A transfer with a transaction in both accounts will be created. (See [transfer payees](https://actualbudget.org/docs/api/reference/#transfers-1).)

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods "Direct link to Methods")

#### `addTransactions` [​](https://actualbudget.org/docs/api/reference/\#addtransactions "Direct link to addtransactions")

`addTransactions(idaccountId, Transaction[]transactions, bool?runTransfers = false, bool?learnCategories = false) → Promise<id[]>`

Adds multiple transactions at once. Does not reconcile (see `importTransactions`). Returns an array of ids of the newly created transactions.

This method does **not** avoid duplicates. Use `importTransactions` if you want the full reconcile behavior.

This method has the following optional flags:

- `runTransfers`: create transfers for transactions where transfer payee is given (defaults to false)
- `learnCategories`: update Rules based on the category field in the transactions (defaults to false)

This method is mainly for custom importers that want to skip all the automatic stuff because it wants to create raw data. You probably want to use `importTransactions`.

#### `importTransactions` [​](https://actualbudget.org/docs/api/reference/\#importtransactions "Direct link to importtransactions")

`importTransactions(idaccountId, Transaction[]transactions, object?opts = {}) → Promise<{ errors, added, updated }>`

Adds multiple transactions at once, while going through the same process as importing a file or downloading transactions from a bank.
In particular, all rules are run on the specified transactions before adding them.
Use `addTransactions` instead for adding raw transactions without post-processing.

The import will "reconcile" transactions to avoid adding duplicates. Transactions with the same `imported_id` will never be added more than once. Otherwise, the system will match transactions with the same amount and with similar dates and payees and try to avoid duplicates. If not using `imported_id` you should check the results after importing.

It will also create transfers if a transfer payee is specified. See [transfers](https://actualbudget.org/docs/api/reference/#transfers).

This method has the following optional flags (passed as the `opts` object):

- `defaultCleared`: whether imported transactions should be marked as cleared (defaults to `true`)
- `dryRun`: if `true`, returns what would be added/updated without actually modifying the database (defaults to `false`)
- `reimportDeleted`: if `true`, transactions that were previously imported and then deleted will be reimported; if `false`, they will be skipped (defaults to `true` for backward compatibility — note that the [file import UI](https://actualbudget.org/docs/transactions/importing#avoiding-duplicate-transactions) defaults to `false`)

Example using opts:

```js
await api.importTransactions(accountId, transactions, {
  reimportDeleted: false,
  defaultCleared: false,
});
```

This method returns an object with the following fields:

- `added`: an array of ids of transactions that were added
- `updated`: an array of ids of transactions that were updated (such as being cleared)
- `errors`: any errors that occurred during the process (most likely a single error with no changes to transactions)

#### `getTransactions` [​](https://actualbudget.org/docs/api/reference/\#gettransactions "Direct link to gettransactions")

`getTransactions(idaccountId, datestartDate, dateendDate) → Promise<Transaction[]>`

Get all the transactions in `accountId` between the specified dates (inclusive). Returns an array of [`Transaction`](https://actualbudget.org/docs/api/reference/#transaction) objects.

#### `updateTransaction` [​](https://actualbudget.org/docs/api/reference/\#updatetransaction "Direct link to updatetransaction")

`updateTransaction(idid, objectfields) → Promise<null>`

Update fields of a transaction. `fields` can specify any field described in [`Transaction`](https://actualbudget.org/docs/api/reference/#transaction).

#### `deleteTransaction` [​](https://actualbudget.org/docs/api/reference/\#deletetransaction "Direct link to deletetransaction")

`deleteTransaction(idid) → Promise<null>`

Delete a transaction.

#### Examples [​](https://actualbudget.org/docs/api/reference/\#examples "Direct link to Examples")

```js
// Create a transaction of $12.00. A payee of "Kroger" will be
// automatically created if it does not exist already and
// assigned to the transaction.

await importTransactions(accountId, [\
  {\
    date: '2019-08-20',\
    amount: 1200,\
    payee_name: 'Kroger',\
    category: 'c179c3f4-28a6-4fbd-a54d-195cced07a80',\
  },\
]);
```

```js
// Get all transactions in an account for the month of August
// (it doesn't matter that August 31st doesn't exist).

await getTransactions(accountId, '2019-08-01', '2019-08-31');
```

```js
// Assign the "Food" category to a transaction

let categories = await getCategories();
let foodCategory = category.find(cat => cat.name === 'Food');
await updateTransaction(id, { category: foodCategory.id });
```

## Accounts [​](https://actualbudget.org/docs/api/reference/\#accounts "Direct link to Accounts")

#### Account [​](https://actualbudget.org/docs/api/reference/\#account "Direct link to Account")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `name` | `string` | yes |  |
| `offbudget` | `bool` | no | Defaults to `false` |
| `closed` | `bool` | no | Defaults to `false` |
| `balance_current` | `number | null` | no | The current balance of the account as reported by bank sync. Can also be set manually. Defaults to `null` |

#### Account Types [​](https://actualbudget.org/docs/api/reference/\#account-types "Direct link to Account Types")

The account type must be one of these valid strings:

- `checking`
- `savings`
- `credit`
- `investment`
- `mortgage`
- `debt`
- `other`

The account type does not affect anything currently. It's simply extra information about the account.

#### Closing Accounts [​](https://actualbudget.org/docs/api/reference/\#closing-accounts "Direct link to Closing Accounts")

Avoid setting the `closed` property directly to close an account; instead use the `closeAccount` method. If the account still has money in it you will be required to specify another account to transfer the current balance to. This will help track your money correctly.

If you want to fully delete an account and remove it entirely from the system, use [`deleteAccount`](https://actualbudget.org/docs/api/reference/#deleteaccount). Note that if it's an on budget account, any money coming from that account will disappear.

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-1 "Direct link to Methods")

#### `getAccounts` [​](https://actualbudget.org/docs/api/reference/\#getaccounts "Direct link to getaccounts")

`getAccounts() → Promise<Account[]>`

Get all accounts. Returns an array of [`Account`](https://actualbudget.org/docs/api/reference/#account) objects.

#### `createAccount` [​](https://actualbudget.org/docs/api/reference/\#createaccount "Direct link to createaccount")

`createAccount(Accountaccount, amount?initialBalance = 0) → Promise<id>`

Create an account with an initial balance of `initialBalance` (defaults to 0). Remember that [`amount`](https://actualbudget.org/docs/api/reference/#primitives) has no decimal places. Returns the `id` of the new account.

#### `updateAccount` [​](https://actualbudget.org/docs/api/reference/\#updateaccount "Direct link to updateaccount")

`updateAccount(idid, objectfields) → Promise<null>`

Update fields of an account. `fields` can specify any field described in [`Account`](https://actualbudget.org/docs/api/reference/#account).

#### `closeAccount` [​](https://actualbudget.org/docs/api/reference/\#closeaccount "Direct link to closeaccount")

`closeAccount(idid, id?transferAccountId, id?transferCategoryId) → Promise<null>`

Close an account. `transferAccountId` and `transferCategoryId` are optional if the balance of the account is 0, otherwise see next paragraph.

If the account has a non-zero balance, you need to specify an account with `transferAccountId` to transfer the money into. If you are transferring from an on budget account to an off budget account, you can optionally specify a category with `transferCategoryId` to categorize the transfer transaction.

Transferring money to an off budget account needs a category because money is taken out of the budget, so it needs to come from somewhere.

If you want to simply delete an account, see [`deleteAccount`](https://actualbudget.org/docs/api/reference/#deleteaccount).

#### `reopenAccount` [​](https://actualbudget.org/docs/api/reference/\#reopenaccount "Direct link to reopenaccount")

`reopenAccount(idid) → Promise<null>`

Reopen a closed account.

#### `deleteAccount` [​](https://actualbudget.org/docs/api/reference/\#deleteaccount "Direct link to deleteaccount")

`deleteAccount(idid) → Promise<null>`

Delete an account.

#### `getAccountBalance` [​](https://actualbudget.org/docs/api/reference/\#getaccountbalance "Direct link to getaccountbalance")

`getAccountBalance(idid, Date?cutoff) → Promise<number>`

Gets the balance for an account. If a cutoff is given, it gives the account balance as of that date. If no cutoff is given, it uses the current date as the cutoff.

#### Examples [​](https://actualbudget.org/docs/api/reference/\#examples-1 "Direct link to Examples")

```js
// Create a savings accounts
createAccount({
  name: "Ally Savings",
  type: "savings
})
```

```js
// Get all accounts

let accounts = await getAccounts();
```

## Categories [​](https://actualbudget.org/docs/api/reference/\#categories "Direct link to Categories")

#### Category [​](https://actualbudget.org/docs/api/reference/\#category "Direct link to Category")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `name` | `string` | yes |  |
| `group_id` | `id` | yes |  |
| `is_income` | `bool` | no | Defaults to `false` |

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-2 "Direct link to Methods")

#### `getCategories` [​](https://actualbudget.org/docs/api/reference/\#getcategories "Direct link to getcategories")

`getCategories() → Promise<Category[]>`

Get all categories.

#### `createCategory` [​](https://actualbudget.org/docs/api/reference/\#createcategory "Direct link to createcategory")

`createCategory(Categorycategory) → Promise<id>`

Create a category. Returns the `id` of the new category.

#### `updateCategory` [​](https://actualbudget.org/docs/api/reference/\#updatecategory "Direct link to updatecategory")

`updateCategory(idid, objectfields) → Promise<null>`

Update fields of a category. `fields` can specify any field described in [`Category`](https://actualbudget.org/docs/api/reference/#category).

#### `deleteCategory` [​](https://actualbudget.org/docs/api/reference/\#deletecategory "Direct link to deletecategory")

`deleteCategory(idid) → Promise<null>`

Delete a category.

### Examples [​](https://actualbudget.org/docs/api/reference/\#examples-2 "Direct link to Examples")

```js
{
  name: "Food",
  group_id: "238d4d38-a512-4e28-9bbe-e96fd5d99251"
}
```

#### Income Categories [​](https://actualbudget.org/docs/api/reference/\#income-categories "Direct link to Income Categories")

Set `is_income` to `true` to create an income category. The `group_id` of the category should point to the existing income group category (currently only one ever exists, see [category group](https://actualbudget.org/docs/api/reference/#category-group)).

## Category Groups [​](https://actualbudget.org/docs/api/reference/\#category-groups "Direct link to Category Groups")

#### Category Group [​](https://actualbudget.org/docs/api/reference/\#category-group "Direct link to Category Group")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `name` | `string` | yes |  |
| `is_income` | `bool` | no | Defaults to `false` |
| `categories` | `Category[]` | no | An array of categories in this group. Not valid when creating or updating a category group<br>Only available in a `get`. |

```js
{
  name: 'Bills';
}
```

#### Income Category Groups [​](https://actualbudget.org/docs/api/reference/\#income-category-groups "Direct link to Income Category Groups")

There should only ever be one income category group,

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-3 "Direct link to Methods")

#### `getCategoryGroups` [​](https://actualbudget.org/docs/api/reference/\#getcategorygroups "Direct link to getcategorygroups")

`getCategoryGroups() → Promise<CategoryGroup[]>`

Get all category groups.

#### `createCategoryGroup` [​](https://actualbudget.org/docs/api/reference/\#createcategorygroup "Direct link to createcategorygroup")

`createCategoryGroup(CategoryGroupgroup) → Promise<id>`

Create a category group. Returns the `id` of the new group.

#### `updateCategoryGroup` [​](https://actualbudget.org/docs/api/reference/\#updatecategorygroup "Direct link to updatecategorygroup")

`updateCategoryGroup(idid, objectfields) → Promise<id>`

Update fields of a category group. `fields` can specify any field described in [`CategoryGroup`](https://actualbudget.org/docs/api/reference/#categorygroup).

#### `deleteCategoryGroup` [​](https://actualbudget.org/docs/api/reference/\#deletecategorygroup "Direct link to deletecategorygroup")

`deleteCategoryGroup(idid) → Promise<null>`

Delete a category group.

## Payees [​](https://actualbudget.org/docs/api/reference/\#payees "Direct link to Payees")

#### Payee [​](https://actualbudget.org/docs/api/reference/\#payee "Direct link to Payee")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `name` | `string` | yes |  |
| `category` | `id` | no |  |
| `transfer_acct` | `id` | no | The `id` of the account this payee transfers to/from, if this is a transfer payee. |

```js
{
  name: "Kroger",
  category: "a1bccbd1-039e-410a-ba05-a76b97a74fc8"
}
```

#### Transfers [​](https://actualbudget.org/docs/api/reference/\#transfers-1 "Direct link to Transfers")

Transfers use payees to indicate which accounts to transfer money to/from. This lets the system use the same payee matching logic to manage transfers as well.

Each account has a corresponding "transfer payee" already created in the system. If a payee is a transfer payee, it will have the `transfer_acct` field set to an account id. Use this to create transfer transactions with [`importTransactions`](https://actualbudget.org/docs/api/reference/#importtransactions).

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-4 "Direct link to Methods")

#### `getPayees` [​](https://actualbudget.org/docs/api/reference/\#getpayees "Direct link to getpayees")

`getPayees() → Promise<Payee[]>`

Get all payees.

#### `getCommonPayees` [​](https://actualbudget.org/docs/api/reference/\#getcommonpayees "Direct link to getcommonpayees")

`getCommonPayees() → Promise<Payee[]>`

Get common payees that appear frequently in transactions.

#### `createPayee` [​](https://actualbudget.org/docs/api/reference/\#createpayee "Direct link to createpayee")

`createPayee(Payeepayee) → Promise<id>`

Create a payee. Returns the `id` of the new payee.

#### `updatePayee` [​](https://actualbudget.org/docs/api/reference/\#updatepayee "Direct link to updatepayee")

`updatePayee(idid, objectfields) → Promise<id>`

Update fields of a payee. `fields` can specify any field described in [`Payee`](https://actualbudget.org/docs/api/reference/#payee).

#### `deletePayee` [​](https://actualbudget.org/docs/api/reference/\#deletepayee "Direct link to deletepayee")

`deletePayee(idid) → Promise<null>`

Delete a payee.

#### `mergePayees` [​](https://actualbudget.org/docs/api/reference/\#mergepayees "Direct link to mergepayees")

`mergePayees(idtargetId, id[]mergeIds) → Promise<null>`

Merge one or more payees into the target payee, retaining the name of the target.

## Tags [​](https://actualbudget.org/docs/api/reference/\#tags "Direct link to Tags")

#### Tag [​](https://actualbudget.org/docs/api/reference/\#tag "Direct link to Tag")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `tag` | `string` | yes |  |
| `color` | `string` | no |  |
| `description` | `string` | no |  |

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-5 "Direct link to Methods")

#### `getTags` [​](https://actualbudget.org/docs/api/reference/\#gettags "Direct link to gettags")

`getTags() → Promise<Tag[]>`

Get all tags.

#### `createTag` [​](https://actualbudget.org/docs/api/reference/\#createtag "Direct link to createtag")

`createTag(Tagtag) → Promise<id>`

Create a tag. Returns the `id` of the new tag.

#### `updateTag` [​](https://actualbudget.org/docs/api/reference/\#updatetag "Direct link to updatetag")

`updateTag(idid, objectfields) → Promise<null>`

Update fields of a tag. `fields` can specify any field described in [`Tag`](https://actualbudget.org/docs/api/reference/#tag).

#### `deleteTag` [​](https://actualbudget.org/docs/api/reference/\#deletetag "Direct link to deletetag")

`deleteTag(idid) → Promise<null>`

Delete a tag.

#### Examples [​](https://actualbudget.org/docs/api/reference/\#examples-3 "Direct link to Examples")

```js
// Create a tag
await createTag({
  tag: 'groceries',
  color: '#ff0000',
  description: 'Grocery shopping expenses',
});
```

```js
// Get all tags
let tags = await getTags();
```

```js
// Update a tag's color
await updateTag(id, { color: '#00ff00' });
```

## Rules [​](https://actualbudget.org/docs/api/reference/\#rules "Direct link to Rules")

#### ConditionOrAction [​](https://actualbudget.org/docs/api/reference/\#conditionoraction "Direct link to ConditionOrAction")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `field` | `string` | yes |  |
| `op` | `string` | yes |  |
| `value` | `string` | yes |  |

#### Rule [​](https://actualbudget.org/docs/api/reference/\#rule "Direct link to Rule")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `stage` | `string` | yes | Must be one of `pre`, `default`, or `post`. |
| `conditionsOp` | `string` | no | Must be one of `and` or `or`. |
| `conditions` | `ConditionOrAction[]` | no |  |
| `actions` | `ConditionOrAction[]` | no |  |

#### Payee Rule [​](https://actualbudget.org/docs/api/reference/\#payee-rule "Direct link to Payee Rule")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `payee_id` | `id` | yes |  |
| `stage` | `string` | yes | Must be one of `pre`, `default`, or `post`. |
| `conditionsOp` | `string` | no | Must be one of `and` or `or`. |
| `conditions` | `ConditionOrAction[]` | no |  |
| `actions` | `ConditionOrAction[]` | no |  |

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-6 "Direct link to Methods")

#### `getRules` [​](https://actualbudget.org/docs/api/reference/\#getrules "Direct link to getrules")

`getRules() → Promise<Rule[]>`

Get all rules.

#### `getPayeeRules` [​](https://actualbudget.org/docs/api/reference/\#getpayeerules "Direct link to getpayeerules")

`getPayeeRules(idpayeeId) → Promise<Rule[]>`

Get all rules associated with `payeeId`.

#### `createRule` [​](https://actualbudget.org/docs/api/reference/\#createrule "Direct link to createrule")

`createRule(Rulerule) → Promise<Rule>`

Create a rule. Returns the new rule, including the `id`.

#### `updateRule` [​](https://actualbudget.org/docs/api/reference/\#updaterule "Direct link to updaterule")

`updateRule(Rulerule) → Promise<Rule>`

Update a rule. Unlike other update methods, this requires the full rule object including `id`. Returns the updated rule.

#### `deleteRule` [​](https://actualbudget.org/docs/api/reference/\#deleterule "Direct link to deleterule")

`deleteRule(idid) → Promise<null>`

Delete a rule.

#### Examples [​](https://actualbudget.org/docs/api/reference/\#examples-4 "Direct link to Examples")

```js
{
  stage: 'pre',
  conditionsOp: 'and',
  conditions: [\
    {\
      field: 'payee',\
      op: 'is',\
      value: 'test-payee',\
    },\
  ],
  actions: [\
    {\
      op: 'set',\
      field: 'category',\
      value: 'fc3825fd-b982-4b72-b768-5b30844cf832',\
    },\
  ],
}
```

## Schedule [​](https://actualbudget.org/docs/api/reference/\#schedule "Direct link to Schedule")

#### Schedule [​](https://actualbudget.org/docs/api/reference/\#schedule-1 "Direct link to Schedule")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `id` | `id` | no |  |
| `name` | `string` | no | Not mandatory but schedule names must be unique. |
| `rule` | `string` | no | All schedules have an associated underlying rule. Not to be supplied with a new schedule. It will be auto created. Rules can not updated to another rule. You can however edit the rule with the API above for Rule. |
| `next_date` | `string` | no | Next occurrence of a schedule. Not to be supplied with a new schedule. |
| `completed` | `boolean` | no | Not to be supplied with a new schedule. |
| `posts_transaction` | `boolean` | no | Whether the schedule should auto-post transactions on your behalf. Defaults to `false`. |
| `payee` | `id | null` | no | Optional; will default to `null`. |
| `account` | `id | null` | no | Optional; will default to `null`. |
| `amount` | `number | { num1: number; num2: number }` | no | Provide only one number, except if the amount uses a isbetween in amountOp, in this case num1 and 2 should be provided. |
| `amountOp` | `'is' | 'isapprox' | 'isbetween'` | no | Controls how `amount` is interpreted. |
| `date` | `date | RecurConfig` | yes | Mandatory field when creating a schedule. If the schedule is a single occurrence just supply the date. otherwise refer to RecurConfig details below. |

#### RecurConfig [​](https://actualbudget.org/docs/api/reference/\#recurconfig "Direct link to RecurConfig")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `frequency` | `'daily' | 'weekly' | 'monthly' | 'yearly'` | yes | How often the schedule repeats. |
| `interval` | `number` | no | The interval at which the recurrence happens. Defaults to `1` if omitted. |
| `patterns` | `RecurPattern[]` | no | Optional patterns to control specific dates for recurrence (e.g. certain weekdays or month days). |
| `skipWeekend` | `boolean` | no | If true, skips weekends when calculating recurrence dates. |
| `start` | `string` | yes | The ISO date string indicating the start date of the recurrence. |
| `endMode` | `'never' | 'after_n_occurrences' | 'on_date'` | yes | Specifies how the recurrence ends: never ends, after a number of occurrences, or on a specific date. |
| `endOccurrences` | `number` | no | Used when `endMode` is `'after_n_occurrences'`. Indicates how many times it should repeat. |
| `endDate` | `string` | no | Used when `endMode` is `'on_date'`. The ISO date string indicating when the recurrence should end. |
| `weekendSolveMode` | `'before' | 'after'` | no | If a calculated date falls on a weekend and `skipWeekend` is true, this controls whether the date moves to the `before` or `after` weekday. |

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-7 "Direct link to Methods")

#### `getSchedules` [​](https://actualbudget.org/docs/api/reference/\#getschedules "Direct link to getschedules")

`getSchedules() → Promise<Schedule[]>`

Get all schedules. Returns an array of [`Schedule`](https://actualbudget.org/docs/api/reference/#schedule) objects.

#### `createSchedule` [​](https://actualbudget.org/docs/api/reference/\#createschedule "Direct link to createschedule")

`createSchedule(Scheduleschedule) → Promise<id>`

Create schedule based on information filled in the schedule object. Please refer to notes of schedule object for details each field.

#### `updateSchedule` [​](https://actualbudget.org/docs/api/reference/\#updateschedule "Direct link to updateschedule")

`updateSchedule(idid, objectfields) → Promise<schedule>`

Update fields of a rule. `fields` can specify any field described in [`Schedule`](https://actualbudget.org/docs/api/reference/#Schedule). Returns the updated rule.

#### `deleteSchedule` [​](https://actualbudget.org/docs/api/reference/\#deleteschedule "Direct link to deleteschedule")

`deleteSchedule(idid) → Promise<null>`

## Misc [​](https://actualbudget.org/docs/api/reference/\#misc "Direct link to Misc")

#### BudgetFile [​](https://actualbudget.org/docs/api/reference/\#budgetfile "Direct link to BudgetFile")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `name` | `string` | yes | The budget's name. |
| `cloudFileId` | `string` | yes | The id for the budget on the server. This is usually a UUID. |
| `groupId` | `string` | yes | The group id for the budget. |
| `hasKey` | `boolean` | yes | If the file has an encryption key. |
| `encryptKeyId` | `string` | no | The encryption key ID for the file, if it is encrypted. |
| `state` | `string` | no | Remote files have this set to "remote". |
| `id` | `string` | no | The local budget file's local ID. |

#### InitConfig [​](https://actualbudget.org/docs/api/reference/\#initconfig "Direct link to InitConfig")

| Field | Type | Required? | Notes |
| --- | --- | --- | --- |
| `serverURL` | `string` | no | The URL of your Actual Budget server. |
| `password` | `string` | no | The password of your Actual Budget server. |
| `dataDir` | `string` | no | The directory to store locally cached budget files. |
| `verbose` | `boolean` | no | Enable/disable logging from actual internals |

#### Methods [​](https://actualbudget.org/docs/api/reference/\#methods-8 "Direct link to Methods")

#### `init` [​](https://actualbudget.org/docs/api/reference/\#init "Direct link to init")

`init(InitConfig?config) → Promise<void>`

Initializes the API by connecting to an Actual Budget server. The config parameter is optional and defaults to `{}` (local-only mode).

#### `shutdown` [​](https://actualbudget.org/docs/api/reference/\#shutdown "Direct link to shutdown")

`shutdown() → Promise<void>`

Shuts down the API. This will close any open budget and clean up any resources.

#### `sync` [​](https://actualbudget.org/docs/api/reference/\#sync "Direct link to sync")

`sync() → Promise<void>`

Synchronizes the locally cached budget files with the server's copy.

#### `runBankSync` [​](https://actualbudget.org/docs/api/reference/\#runbanksync "Direct link to runbanksync")

`runBankSync({ stringaccountId }) → Promise<void>`

Run the 3rd party (GoCardless, SimpleFIN) bank sync operation. This will download the transactions and insert them into the ledger.

#### `runImport` [​](https://actualbudget.org/docs/api/reference/\#runimport "Direct link to runimport")

`runImport(stringbudgetName, funcfunc) → Promise<void>`

Creates a new budget file with the given name, and then runs the custom importer function to populate it with data.

#### `getBudgets` [​](https://actualbudget.org/docs/api/reference/\#getbudgets "Direct link to getbudgets")

`getBudgets() → Promise<BudgetFile[]>`

Returns a list of all budget files either locally cached or on the remote server. Remote files have a `state` field and local files have an `id` field.

#### `loadBudget` [​](https://actualbudget.org/docs/api/reference/\#loadbudget "Direct link to loadbudget")

`loadBudget({ stringsyncId }) → Promise<void>`

Load a locally cached budget file.

#### `downloadBudget` [​](https://actualbudget.org/docs/api/reference/\#downloadbudget "Direct link to downloadbudget")

`downloadBudget({ stringsyncId, string?password }) → Promise<void>`

Load a budget file. If the file exists locally, it will load from there. Otherwise, it will download the file from the server.

#### `batchBudgetUpdates` [​](https://actualbudget.org/docs/api/reference/\#batchbudgetupdates "Direct link to batchbudgetupdates")

`batchBudgetUpdates(funcfunc) → Promise<void>`

Performs a batch of budget updates. This is useful for making multiple changes to the budget in a single call to the server.

#### `runQuery` [​](https://actualbudget.org/docs/api/reference/\#runquery "Direct link to runquery")

`runQuery({ ActualQLquery }) → Promise<unknown>`

Allows running any arbitrary ActualQL query on the open budget.

#### `getIDByName` [​](https://actualbudget.org/docs/api/reference/\#getidbyname "Direct link to getidbyname")

`getIDByName({ stringtype, stringstring }) → Promise<string>`

get the ID for any Account, Payee, Category or Schedule by providing the corresponding name. Allowed types are 'accounts', 'schedules', 'categories', 'payees'.

#### `getServerVersion` [​](https://actualbudget.org/docs/api/reference/\#getserverversion "Direct link to getserverversion")

`getServerVersion() → Promise<{error?: string;} | {version: string;}>`

return error or the current server versions.

- [Types of Methods](https://actualbudget.org/docs/api/reference/#types-of-methods)
- [Primitives](https://actualbudget.org/docs/api/reference/#primitives)
- [Budgets](https://actualbudget.org/docs/api/reference/#budgets)
- [Transactions](https://actualbudget.org/docs/api/reference/#transactions)
- [Accounts](https://actualbudget.org/docs/api/reference/#accounts)
- [Categories](https://actualbudget.org/docs/api/reference/#categories)
  - [Examples](https://actualbudget.org/docs/api/reference/#examples-2)
- [Category Groups](https://actualbudget.org/docs/api/reference/#category-groups)
- [Payees](https://actualbudget.org/docs/api/reference/#payees)
- [Tags](https://actualbudget.org/docs/api/reference/#tags)
- [Rules](https://actualbudget.org/docs/api/reference/#rules)
- [Schedule](https://actualbudget.org/docs/api/reference/#schedule)
- [Misc](https://actualbudget.org/docs/api/reference/#misc)